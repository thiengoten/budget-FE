export default function HomePage() {
  return (
    <>
      {/* <h1>{String(isOpen)}</h1>
      <Button variant='default' onClick={setIsOpen}>
        Toggle
      </Button> */}
      <div>Home</div>
    </>
  )
}
